name: MSYS2 PKGBUILD build test

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * 3"
  workflow_dispatch:

jobs:
  win-makepkg:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { icon: "🟦", sys: mingw64, env: x86_64 }
    name: 🚧${{ matrix.icon }} ${{ matrix.sys }} | makepkg
    defaults:
      run:
        shell: msys2 {0}
    env:
      MINGW_ARCH: ${{ matrix.sys }}
    steps:
      - name: "⚙️ git config"
        run: git config --global core.autocrlf input
        shell: bash

      - name: "🧰 Checkout"
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: "${{ matrix.icon }} Setup MSYS2"
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.sys }}
          update: true
          install: >
            git
            base-devel
            tree
            mingw-w64-${{ matrix.env }}-toolchain

      - name: "🚧 Build package"
        run: |
          cd ./pkgbuild/msys2/
          makepkg-mingw --noconfirm --noprogressbar -sCLf

      - name: Export Package Name
        run: echo "across_package_name=$(ls pkgbuild/msys2/ | grep pkg.tar.zst)" >> $GITHUB_ENV

      - name: "📤 Upload artifact: package"
        uses: actions/upload-artifact@v2
        with:
<<<<<<< HEAD
<<<<<<< HEAD
          name: ${{ env.across_package_name }}
=======
          name: ${{ matrix.sys }}-across-dev-git
>>>>>>> ae559fc (update package name)
=======
          name: ${{ env.across_package_name }}
>>>>>>> 7c90883 (sync master branch)
          path: ./pkgbuild/msys2/*${{ matrix.env }}*.zst

      - name: "🔍 Show package content"
        run: |
          mkdir tmp
          cd tmp
          sha256sum ../pkgbuild/msys2/*.zst
          tar -xf ../pkgbuild/msys2/*.zst
          tree .
          cd ../
<<<<<<< HEAD
          rmdir ./tmp --ignore-fail-on-non-empty
<<<<<<< HEAD
=======

  win-test:
    needs: win-makepkg
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { icon: "🟦", sys: mingw64, env: x86_64 }
    name: 🚦${{ matrix.icon }} ${{ matrix.sys }} | test
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - name: "${{ matrix.icon }} Setup MSYS2"
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.sys }}
          update: true

      - name: "📥 Download artifact: package"
        uses: actions/download-artifact@v2
        with:
          path: .
          name: ${{ matrix.sys }}-across-dev-git

      - name: "🛠️ Install package"
        run: pacman -U --noconfirm --noprogressbar *.zst

      - name: "🚦 Test package"
<<<<<<< HEAD
        run: TOOL
>>>>>>> 5c25bb2 (remove mingw32 test)
=======
        run: sha256sum *.zst
>>>>>>> 5827d6d (fix test package run)
=======
          rmdir ./tmp --ignore-fail-on-non-empty
>>>>>>> 7c90883 (sync master branch)
